using AIAssistant.Customizor.Main;

namespace AIAssistant.Customizor.Incubator
{
    public static class IncubatorEditor
    {
        // Keep intermediate signals private to each Incubator idea or feature.
        // Shared cross-module signals must be promoted to Workplace and then exposed
        // through SignalTickFrame producers or consumers.
        public static void Apply(SignalTickFrame signalTickFrame)
        {
        }
    }
}
