namespace MapEditorTool.SignalWeaver.Featuror
{
    public static class FeaturorEditor
    {
        // Mature feature families live under Featuror, but each feature is still
        // wired explicitly by the owning ViewModel/SignalWeaver host until a
        // shared MapEditorTool signal frame exists.
    }
}
